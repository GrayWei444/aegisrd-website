---
// AegisID 技術頁 — 規格 v1.2 §3.5（骨骼比率深度研究不上網站 — 專利前不公開）
import Base from '../../layouts/Base.astro';
---
<Base
  title="AegisID — 裝置無關的生物身份技術"
  description="AegisID：AuraFace（ResNet100+ArcFace）512 維臉部特徵向量、YuNet 五點對齊、多角度註冊融合。FEI 受控測試 FRR 0%、LFW 843 萬組比對。僅存向量、不存照片。"
  active="technology"
>
  <section class="hero" style="min-height:55vh">
    <div class="hero-content">
      <div class="hero-badge">🪪 AegisID</div>
      <h1>你的身份，<span class="gradient">不綁在手機上</span></h1>
      <p>換手機、掉手機、被偷手機 —— 刷個臉加一組 PIN，帳號與好友原地回來。而我們從頭到尾沒存過你的照片。</p>
    </div>
  </section>

  <section class="ai-engine">
    <div class="container">
      <div class="section-label">// PIPELINE</div>
      <h2 class="section-title reveal">辨識管線</h2>
      <div class="ai-layers" style="max-width:900px">
        <div class="ai-layer reveal">
          <div class="layer-badge l1">1</div>
          <div class="layer-info">
            <h4>偵測與對齊 — YuNet 五點對齊</h4>
            <p>臉部偵測後以五個關鍵點做幾何對齊 —— 歪頭、距離、角度差異在進模型前就被標準化。</p>
          </div>
        </div>
        <div class="ai-layer reveal">
          <div class="layer-badge l2">2</div>
          <div class="layer-info">
            <h4>特徵抽取 — AuraFace（ResNet100 + ArcFace）</h4>
            <p>臉部影像 → 512 維特徵向量（L2 正規化）。比對用 cosine 相似度 —— 影像本身用完即棄，永不儲存、永不上傳。</p>
          </div>
        </div>
        <div class="ai-layer reveal">
          <div class="layer-badge l3">3</div>
          <div class="layer-info">
            <h4>註冊強化 — 多角度中位數融合</h4>
            <p>註冊時取多角度樣本做中位數融合，一次註冊、長期穩定 —— 光線與姿態變化不再讓你被自己的帳號拒之門外。</p>
          </div>
        </div>
        <div class="ai-layer reveal">
          <div class="layer-badge l1">4</div>
          <div class="layer-info">
            <h4>活體防偽</h4>
            <p>轉頭活體挑戰 + CNN 防偽模型 —— 照片、螢幕翻拍、面具在特徵抽取之前就被擋下。</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="cost">
    <div class="container">
      <div class="section-label">// VALIDATION</div>
      <h2 class="section-title reveal">實測數據</h2>
      <div class="problem-stat">
        <div class="stat-card reveal"><div class="number" style="background:linear-gradient(135deg,var(--green),var(--blue));-webkit-background-clip:text;background-clip:text">FRR 0%</div><div class="desc">FEI 資料集受控測試 — 本人零誤拒</div></div>
        <div class="stat-card reveal"><div class="number" style="background:linear-gradient(135deg,var(--green),var(--blue));-webkit-background-clip:text;background-clip:text">843 萬組</div><div class="desc">LFW 資料集大規模比對驗證</div></div>
        <div class="stat-card reveal"><div class="number" style="background:linear-gradient(135deg,var(--green),var(--blue));-webkit-background-clip:text;background-clip:text">512 維</div><div class="desc">特徵向量 — 僅存向量，不存照片</div></div>
      </div>
    </div>
  </section>

  <section class="moat">
    <div class="container">
      <div class="section-label">// PRIVACY</div>
      <h2 class="section-title reveal">隱私聲明</h2>
      <div class="moat-col highlight reveal" style="max-width:760px">
        <ul>
          <li><span class="check">✓</span> 僅儲存 512 維特徵向量 —— 無法還原成人臉影像</li>
          <li><span class="check">✓</span> 照片與影像幀用完即棄，永不上傳</li>
          <li><span class="check">✓</span> 身份資料（好友、金鑰）以你的 PIN 加密後才離開裝置 —— 沒有 PIN，誰都解不開</li>
          <li><span class="check">✓</span> 忘記 PIN？Shamir 門檻式社交恢復 —— 好友合力才能救回，任何單一方（包括我們）都不行</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>AegisID 已內建於 AegisTalk</h2>
    <a href="/aegistalk" class="btn-primary">看它怎麼保護你的帳號 →</a>
  </section>
</Base>
