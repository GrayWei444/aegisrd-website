---
// AegisAI 技術頁 — 規格 v1.2 §3.5（26 維對話特徵；50 維流量特徵未實作不上網站）
import Base from '../../layouts/Base.astro';
---
<Base
  title="AegisAI — 三層防詐引擎技術細節"
  description="AegisAI：L1 黑白名單 <1ms、L2 專家規則 26 維對話特徵 × fastText 融合、L3 行為基線。53,640 條規則、98.5% 偵測率、F1 0.981（域內測試）。AegisTrack 情報閉環。"
  active="technology"
>
  <section class="hero" style="min-height:55vh">
    <div class="hero-content">
      <div class="hero-badge">🧠 AegisAI Core</div>
      <h1>三層防護<span class="gradient">引擎</span></h1>
      <p>已知威脅毫秒攔截、新話術語意抓取、行為異常本地偵測 —— 全部跑在使用者裝置上。</p>
    </div>
  </section>

  <section class="ai-engine">
    <div class="container">
      <div class="section-label">// THREE LAYERS</div>
      <h2 class="section-title reveal">逐層拆解</h2>
      <div class="ai-layers" style="max-width:900px">
        <div class="ai-layer reveal">
          <div class="layer-badge l1">L1</div>
          <div class="layer-info">
            <h4>黑白名單 — 已知威脅即時攔截</h4>
            <p>數十萬筆惡意域名黑名單、白名單、假冒域名（typosquatting）偵測、可疑 TLD 規則 —— 純查表與字串比對，攔截延遲小於 1 毫秒。</p>
            <div class="speed">&lt; 1ms</div>
          </div>
        </div>
        <div class="ai-layer reveal">
          <div class="layer-badge l2">L2</div>
          <div class="layer-info">
            <h4>專家規則 × fastText 語意融合</h4>
            <p>53,640 條可解釋規則（關鍵詞、話術片語、詐騙場景），與 fastText 語意模型（&lt;6MB）雙引擎融合取高分 —— 規則給出「為什麼危險」的解釋，ML 補抓規則沒見過的變形話術。26 維對話特徵參與評估。</p>
            <div class="speed">98.5% 偵測率 · 99.6% 準確率 · F1 0.981（26,137 則實測訊息，域內測試）</div>
          </div>
        </div>
        <div class="ai-layer reveal">
          <div class="layer-badge l3">L3</div>
          <div class="layer-info">
            <h4>行為基線 — Isolation Forest 異常偵測</h4>
            <p>訊息頻率、回覆延遲、金錢詞頻、深夜比例等 26 維行為特徵，在<b>本地</b>訓練個人互動基線 —— 「聊了三週突然開口借錢」這種單則訊息看不出的異常，行為層看得見。</p>
            <div class="speed">100% 本地訓練 — 行為資料不出裝置</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="cost">
    <div class="container">
      <div class="section-label">// INTELLIGENCE LOOP</div>
      <h2 class="section-title reveal">AegisTrack 情報閉環</h2>
      <p class="section-desc reveal">規則不是一次寫完的 —— 是一條每天在跑的生產線。</p>
      <div class="problem-chain" style="margin-top:2rem">
        <div class="chain-step"><div class="icon">🕷️</div><div class="label">爬取</div><div class="sub">詐騙情報來源</div></div>
        <div class="chain-arrow" style="color:var(--accent)">→</div>
        <div class="chain-step"><div class="icon">🔬</div><div class="label">分析</div><div class="sub">話術拆解歸類</div></div>
        <div class="chain-arrow" style="color:var(--accent)">→</div>
        <div class="chain-step"><div class="icon">📝</div><div class="label">寫規則</div><div class="sub">產出可解釋規則</div></div>
        <div class="chain-arrow" style="color:var(--accent)">→</div>
        <div class="chain-step"><div class="icon">🚀</div><div class="label">部署</div><div class="sub">同步到所有裝置</div></div>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>想看它在產品裡怎麼動？</h2>
    <a href="/aegistalk" class="btn-primary">AegisTalk 的三層防護 →</a>
  </section>
</Base>
